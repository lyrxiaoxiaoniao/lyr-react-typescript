### 1 gulp 自动化部署

```
    1. 全局安装 gulp 和 gulp-fez-sftp：
    $ cnpm install --global gulp
    $ cnpm install --global gulp-fez-sftp

    2. 作为项目的开发依赖（devDependencies）安装：
    $ cnpm install --save-dev gulp
    $ cnpm install --save-dev gulp-fez-sftp

```
